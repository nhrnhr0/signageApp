server {
        listen 80;
        server_name bet-aful-screens-backend.bizbiz.co.il;
        client_max_body_size 5M;

        location / {
                include proxy_params;
                proxy_pass http://unix:/var/run/betaful_screen_gunicorn.sock;

        }

        location /static {
                autoindex on;
                alias /home/ubuntu/clients/betaful/backend/static;
        }
        location /media {
                autoindex on;
                alias /home/ubuntu/clients/betaful/backend/static/media_root;
        }
}

